# Прямі HTTP-виклики, без обгорток (python-binance / ccxt заборонені)
fastapi>=0.110
uvicorn[standard]>=0.27
httpx>=0.27
websockets>=12
numpy>=1.26
# База часових поясів для zoneinfo (Europe/Kyiv) — обовʼязкова на Windows,
# де немає системної tzdata
tzdata>=2024.1
